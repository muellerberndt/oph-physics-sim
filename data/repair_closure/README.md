# Bounded repair-closure receipt

`bounded_atomic_self_readback_closure_receipt.json` is generated by
`oph_fpe.dynamics.self_readback_repair_closure`.

`record_counting_source_projection.json` pins the issue-628 full-pile source
statement and upstream manifest hashes consumed by the total-twelve audit.
The projection does not identify the new complete-balancing macro-event with
the upstream packet's atomic one-unit record move.

The receipt uses exact integer and rational arithmetic. It covers the finite
twelve-port scalar event grammar, protected totals zero through four, and the
complete nonnegative total-twelve sector. A signed control covers all
\(\{-1,0,1\}^{12}\) states. Its verifier is a deterministic producer replay,
not an independent implementation. The positive fixed-point receipt concerns
the one-step generator ray. IID path closure remains conditional on proposed
free-composition and temporal-completeness clauses. The receipt does not
certify a physical clock, a complete noncommutative repair grammar, refinement
naturality, protected-sector selection, or a universe-selection theorem.

Regenerate and verify it from the repository root:

```bash
python3 -m oph_fpe.dynamics.self_readback_repair_closure \
  --output data/repair_closure/bounded_atomic_self_readback_closure_receipt.json
python3 -m oph_fpe.dynamics.self_readback_repair_closure \
  --verify data/repair_closure/bounded_atomic_self_readback_closure_receipt.json
```

## Same-action seam scale

`seam_current_same_metric_scale_receipt.json` composes the exact unit-current
`D6` action with the response-selected Gram on that same record carrier. It
certifies

\[
  \lVert\delta_{\rm edge}\rVert_G^2=2-2/\sqrt5,
  \qquad 1<\lVert\delta_{\rm edge}\rVert_G^2<6/5.
\]

This is a source-native dimensionless scale inside the pinned internal
completion. Source selection of the signed record as physical position and of
the A2 response topology remains open. The raw FZ-12 chart norm is four; the
response normalization divides by the squared raw port radius
`5/2+sqrt(5)/2`. A common conversion from response units to physical length
remains freely rescalable, so the packet does not supply an SI scale, a
physical `kappa_edge`, a physical lower bound, or comparison permission.

Regenerate and verify the packet from the repository root:

```bash
python3 -m oph_fpe.dynamics.seam_current_same_metric_scale
python3 -m oph_fpe.dynamics.seam_current_same_metric_scale \
  --verify data/repair_closure/seam_current_same_metric_scale_receipt.json
python3 -m oph_fpe.dynamics.verify_seam_current_same_metric_scale_independent
```
