"""Observer-frame quantum statistics probe (lane D3).

Exploratory, non-evidential. Born weights of the eight committed PR-04
context effects are reproduced as integer count ratios over a finite
deterministic record ensemble driven by the declared branch tables, which
are transcriptions of the committed conditional weights Tr(p F)
(``DESIGN.md`` section 3.3), with no probability postulate on the code
path; collapse is modeled as conditioning on the observer's record.
Design, sources, conventions, and boundaries are pinned in
``oph_fpe/qm_observer/DESIGN.md``. No instrument is frozen, no run is
evidential, INS-01 remains the controlling OL-A1 verdict, and OL-C5 status
is untouched by this lane.
"""
